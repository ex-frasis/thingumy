<h3 class="exfrasis-heading artist-heading">
  Featured Artists
</h3>

<div class="artist-list">
  <% Artist.order(:first_name).each do |artist| %>
    <div class="artist-index-row">
      <%= link_to artist.to_s, "/artwork/#{artist.artwork.try(:slug)}" %>
    </div>
  <% end %>
</div>
